{
    "swagger": "2.0",
    "info": {
        "description": "{{.Description}}",
        "title": "{{.Title}}",
        "contact": {},
        "license": {},
        "version": "{{.Version}}"
    },
    "host": "{{.Host}}",
    "basePath": "{{.BasePath}}",
    "paths": {
        "/kill": {
            "get": {
                "description": "Kill all the running tasks",
                "summary": "Kill all the running tasks",
                "responses": {
                    "200": {
                        "description": "kill all the tasks",
                        "schema": {
                            "type": "string"
                        }
                    }
                }
            }
        }
    }
}